[package]
name = "skillbox-cli"
version = "0.2.0"
edition = "2021"

[dependencies]
serde = "1"
serde_json = "1"
skillbox-core = { path = "../skillbox-core" }
skillbox-github = { path = "../skillbox-github" }
